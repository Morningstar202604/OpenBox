# ============================================================
# OpenBox 环境变量示例
# 复制为 .env 并按需调整；切勿提交真实 .env
# ============================================================

# ===== Supabase（前端 / Vite）=====
# 用途：启用「社区投稿」云端审核库 + 登录/云端收藏/验证/评论/反馈。
#       不配置则全站走本地种子，投稿/收藏/评论仅存本机（localStorage）兜底。
# 获取：Supabase 控制台 → Project Settings → API → Project URL 与 anon/publishable public key
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ===== Supabase 代理（可选，国内加速）=====
# 国内直连 supabase.co 延迟高（300-800ms），可通过 Cloudflare Worker 反代加速。
# 配置方法：
#   1. 在 Cloudflare 创建 Worker，代理转发到你的 Supabase URL
#   2. 将 Worker 的 URL 填在这里（如 https://your-proxy.workers.dev）
# 不配置则直连 Supabase（默认行为）。
# VITE_SUPABASE_PROXY_URL=https://your-proxy.workers.dev
