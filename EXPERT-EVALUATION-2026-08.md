# OpenBox 专家评估与开发路线图（2026-08）

> 评估形式：模拟 67 人专家评审团（8 个职能组），基于对代码库全量审计（src 98 文件 / 约 9,400 行、CI/CD 工作流、Supabase 迁移链 0001–0008、scripts 工具链）、部署链路核查与 2026 年国内免费托管渠道实测调研。
> 评估前提约束：**个人开发者 · 资金紧张 · 目标场景为学校**。所有建议均以此为第一优先级。

---

## 0. 评分卡总览

| 维度 | 得分 | 一句话结论 |
|---|---|---|
| 代码质量与工程素养 | ★★★★☆ 4.5 | 远超个人项目平均水平，接近成熟开源项目 |
| 架构可扩展性 | ★★★★☆ 4.0 | 可选后端三级降级设计教科书级；data.ts 上帝模块待拆 |
| 性能 | ★★★★☆ 4.0 | 五路分包+三层懒加载到位；种子数据随规模线性膨胀是隐患 |
| 测试与质量保障 | ★★★☆☆ 3.0 | 纯逻辑层覆盖好；组件/store/云同步零测试 |
| **部署可达性（大陆）** | ★★☆☆☆ 2.0 | **最大短板：pages.dev 大陆基本不可达，自动部署处于禁用状态** |
| 市场竞争力 | ★★★☆☆ 3.0 | AI 导航红海同质化严重，但社区验证机制是真差异化 |
| 校园场景匹配度 | ★★★★☆ 4.0 | 免费/白嫖定位与学生群体天然契合，缺的是针对性改造 |
| 合规风险（校园语境） | ★★☆☆☆ 2.5 | 「代理节点」等分类在校内传播有合规隐患，需可配置隐藏 |

**总裁决：项目底子很好，当前卡点不是"写得不好"，而是"上不去（大陆不可达）、打不准（没有校园针对性）"。未来 30 天的全部精力应投向这两点，而不是继续堆功能。**

---

## 1. 各专家组评估意见

### 1.1 产品组（9 人）

**市场现状判断：**
- AI 资源导航是 2024–2026 的绝对红海：通用导航站（ai-bot.cn 类）、GitHub awesome-list、各类"白嫖站"数以百计，同质化率极高。**靠"收录多"没有出路**——280 条 vs 别人 2000 条，用户感知不到差异。
- 本项目真正稀缺的能力有两个：
  1. **社区验证机制**（还能用/已失效投票 + 每日机器巡检 + 死链黑名单）——直击免费资源"高死亡率"这一领域最大痛点，这是竞品普遍没做或做不好的；
  2. **数据卫生流水线**（四重去重、存活白名单、11 种验证方法交叉核验）——这是内容型产品的护城河。

**战略共识：不做"更大的导航站"，做"最可信的免费资源站"。信任 > 数量。**

**校园切入逻辑（关键增量判断）：**
- 学生是"免费 AI 资源"需求密度最高、付费意愿最低、传播速度最快的人群——三者叠加使校园成为本项目**唯一应该优先打的战场**；
- 校园获客成本趋近于零（社团、宿舍、新生季），而公网冷启动一个导航站几乎不可能获得自然流量；
- 结论：把 OpenBox 从"公网导航站的第 N 名"重新定位为"**校园白嫖资源入口第一名**"，先在一个学校做透，再横向复制到其他学校（每所学校一套轻量定制即可复用同一套代码）。

**产品组给出的定位一句话：**
> 「你学校里最好用的免费 AI 资源站——每条都验证过，失效了大家投票告诉你」

---

### 1.2 架构组（10 人）

**值得肯定（保持，勿动）：**
- Supabase 可选接入的三级降级（占位符配置 → 运行时 `hasSupabase` 判定 → `withSupabase()` 统一兜底包装器）——纯静态 fork 即跑全功能，这正是低成本部署最需要的设计；
- `taxonomy.ts` 单一数据源驱动路由/导航/评分维度——扩展成本极低，为"校园版定制"提供了现成抓手；
- 数据卫生管道（seed.ts 四重去重 + blacklist.ts 121 死链域名 + data-integrity 测试锁不变量）。

**必须解决的问题（按危害排序）：**

| # | 问题 | 位置 | 危害 | 建议 |
|---|---|---|---|---|
| A1 | 种子数据全量打进首屏 bundle | `sites-data` chunk | 资源涨到 1000+ 条时移动端首屏明显变慢 | 构建期拆分为按分类的静态 JSON，首页只加载默认分类 |
| A2 | 搜索为全量内存线性扫描 | `lib/data.ts` getResources | 数据 ×3 后搜索延迟可感知 | 中期：构建期生成简易倒排索引（title/tags/拼音首字母），几十 KB 即可 |
| A3 | `lib/data.ts` 671 行上帝模块 | resources/submissions/comments/reports/admin 六域耦合 | 改一处怕碰全处 | 按域拆四个文件，接口签名不变 |
| A4 | 收藏云同步待删队列无测试 | `useFavoritesStore.ts` | 全项目最复杂的并发逻辑裸奔 | P1 补测试后再动它 |
| A5 | i18n `dict` 为可变全局单例，切换正确性依赖调用时序约定 | `i18n/translations.ts` | 并发 loadLocale 有竞态可能 | 给 loadLocale 加进行中 Promise 缓存（约 10 行） |

---

### 1.3 前端体验组（8 人）

- **PWA 宣传与实现不符（最高优先级）**：slogans 宣称"PWA 离线可用"，实际无 Service Worker，仅到"可安装"级别。补 `vite-plugin-pwa`（precache app shell + runtime cache）即可闭环，约半天工作量。**校园弱网/宿舍断网场景下，离线能力是真实刚需而非锦上添花**。
- 移动端体验（底部 Tab、safe-area、触控目标）已是同类项目中上游，无需大改。
- 建议增加：资源卡片「复制链接」一键分享（校园场景主要靠微信/QQ 群转发传播）。
- 三语界面（zh/en/ja）对校园场景价值有限但维护成本不低——**不建议删除**（是开源项目的国际化门面），但迭代顺序降到最低，新功能允许只出中文文案（zh 字典兜底机制已支持）。

---

### 1.4 DevOps 部署组（12 人）★ 本次评估核心输出 ★

**现状三个硬伤：**
1. `deploy.yml` 自动触发已被注释禁用（commit `25c40b7`），push main 不再上线，生产与仓库脱节；
2. 主域名 `openbox-nav-5ke.pages.dev` 在大陆长期 DNS 污染/SNI 干扰，**目标用户（学生）根本打不开**——这对"部署给学校用"是致命的；
3. Supabase 直连延迟 300–800ms 且不稳定，云功能在国内形同虚设（所幸降级设计保证了核心浏览不受影响）。

**三套 $0 方案对比（均已按 2026-08 实际政策核实）：**

| | 方案 A：校内网部署 ⭐推荐 | 方案 B：腾讯 EdgeOne Pages ⭐推荐 | 方案 C：Cloudflare Pages（现状） |
|---|---|---|---|
| 成本 | ¥0（用校内闲置机器/虚拟机） | ¥0（免费版长期有效） | ¥0 |
| 大陆/校园访问速度 | 内网秒开，不受任何墙影响 | 好（腾讯自有网络，大陆边缘节点） | 差（pages.dev 基本不可达；绑免费域名后勉强可用） |
| 备案 | 不需要 | 默认 `*.edgeone.app` 域名不需要；自定义域走大陆加速才需要 | 不需要 |
| 部署方式 | scp + Caddy/Nginx，10 分钟 | 控制台导入 Git 仓库自动构建 | 已配好但被禁用 |
| 适用 | **学校内用户为主** | 公网演示 + 外网同学访问 | 海外镜像/备份 |
| 风险 | 毕业后机器回收（迁移容易，纯静态） | 免费额度政策变动 | 政策性不可控 |

**EdgeOne Pages 关键事实（本次调研核实）：**
- 免费套餐：安全加速**流量不限量、请求不限量**，长期有效，无超额付费；
- 官方明确欢迎对象包含"**个人兴趣开发者、教育类非营利项目**"——与本项目完全对口；
- 提供 Git 仓库集成（推送自动构建部署），支持自定义 build command，本项目现有 `VITE_BASE_URL=/ VITE_BUILD_DIR=dist npm run build` 直接可用；
- 自带免费 SSL、DDoS/WAF 基础防护。

**部署组的最终建议：A + B 双轨并行。**
- 校内机器跑一份（主力，服务本校同学）；
- EdgeOne Pages 跑一份（公网门面，供校外/毕业后访问，也是 GitHub 开源 README 的在线 demo）；
- Cloudflare 那份保留作海外镜像，不再投入精力。

**配套改造（详见路线图 P0）：**
- 恢复一条自动部署链路（EdgeOne Git 集成即零配置解决，无需依赖被禁用的 Actions）；
- `monitor.yml` 注释与现实不符处修正（巡检提交不再触发部署，文案要改）。

---

### 1.5 安全合规组（7 人）

1. **【校园特有·高优先】分类可见性必须可配置**：现收录「代理节点」「中转站」等分类，在公网属灰色地带，**在学校网络环境里主动传播则风险等级完全不同**（校园网有上网行为审计，出问题会牵连个人甚至指导老师）。建议实现 `VITE_HIDDEN_CATEGORIES=proxy,...` 构建期过滤（taxonomy 已是单一数据源，改动集中在 `taxonomy.ts` 过滤 + 类型收窄，约 1 天）。校内版默认隐藏敏感分类，公网版保持全量。
2. UGC 匿名评论/投稿在校内场景建议开启更严格的长度限制与频率限制（0008 迁移已有 DB 级限流，够用）；校内版可直接关闭登录云同步，纯静态运行，**零后端 = 零数据合规负担**。
3. `VITE_ADMIN_EMAILS` 打进产物属轻微信息泄露（RLS 有兜底），顺手改为运行时从 Supabase `admin_emails` 表判定即可。
4. 校内 HTTPS：Caddy 自动证书或学校内部 CA；若纯 HTTP 内网使用，务必在页面明示"校内非加密环境请勿提交真实邮箱"。

---

### 1.6 教育行业顾问组（6 人）

- **落地路径**：找计算机协会/开源社团共建 → 以"社团官方资源站"名义上线 → 新生季（9 月）迎新物料植入二维码。9 月正是窗口期，**P0/P1 必须在开学两周内完成**。
- **功能建议（按 ROI 排序）**：
  1. 新增「新生工具包」场景包（现有 scenarios 机制加一组即可，零架构改动）：选课工具、校园网使用、正版软件渠道、学习资料；
  2. 资源详情页「适用场景」标签强化（考研/竞赛/毕设/日常）；
  3. 每周精选自动生成分享卡片（后期）。
- **运营真相**：导航站留存靠"每周都有新东西"。现有 weekly.ts 每周更新机制是对的，但需要绑定一个固定发布节奏（如每周日晚更新 + 社群预告），内容运营比功能开发更重要。

---

### 1.7 数据/AI 组（8 人）

- `monitor.mjs` 带记忆巡检引擎（连续 2 次失败才判死、DNS 层复核、防误杀 suspect 态）是**全项目最有资产价值的部件**，竞品抄不走，持续投入；
- 现状 ok 226 / unstable 25 / dead 23 —— 死链率约 9%，健康；建议把「最近验证时间」在卡片上更显眼地露出，把"我们真的在验证"变成用户可感知的差异点；
- 约 79 条资源本地无法直连核验的问题，在校园网环境下反而部分缓解（教育网出口不同），可借部署契机用校内线路重跑一轮 check:links。

---

### 1.8 增长运营组（7 人）

- CSR-only 导致百度基本不收录资源详情页——短期接受现状（校园传播不走搜索引擎），中期用构建期预渲染解决（P2）；
- GitHub 开源面（README 徽章、截图、双语）质量高，是简历/作品集素材，维持；
- 校园增长飞轮：**社群（QQ/微信群）→ 每周资源速递 → 投票/评论互动 → UGC 反哺内容**。技术上只需保证"分享出去的链接打开快"（回到部署问题）和"卡片好看"（分享海报，P2）。

---

## 2. 战略方向（全体表决通过）

```
一句话：从「公网 AI 导航站之一」转型为「校园免费 AI 资源的可信入口」，
       以单所学校为试点打磨"部署包"，再逐校复制。

三条铁律（未来 90 天）：
1. 一切开发服务于"校园可达 + 校园可信"，暂停新的大而全功能；
2. 保持纯静态可运行（Supabase 永远可选），让任何学校零门槛复制部署；
3. 内容真实性流水线（monitor 巡检）是最深护城河，永不降级。
```

---

## 3. 开发路线图

### P0 · 部署解困周（本周必做，全部 ≤ 半天粒度）

| # | 任务 | 产出/验收 |
|---|---|---|
| P0-1 | EdgeOne Pages 接入：控制台导入 Git 仓库，build command `VITE_BASE_URL=/ VITE_BUILD_DIR=dist npm run build`，输出 `dist` | 公网可达的 `*.edgeone.app` 地址 |
| P0-2 | 校内部署包：`Caddyfile`（SPA fallback + assets immutable 缓存头）+ `DEPLOY-SCHOOL.md` 操作手册 | 一台校内机器 10 分钟起服务 |
| P0-3 | `VITE_HIDDEN_CATEGORIES` 构建期分类过滤（安全合规项） | 校内版产物不含代理节点分类 |
| P0-4 | README/HANDOFF 与现实对齐：hash 路由描述过时、monitor 触发链路失效、wrangler 目录口径二义 | 文档一致性 |
| P0-5 | 用 `VITE_SUPABASE_URL=`（空）构建校内版，确认纯静态模式全功能可用 | 校内版零后端依赖 |

### P1 · 校园版改造（第 2–4 周，赶 9 月开学窗口）

| # | 任务 | 说明 |
|---|---|---|
| P1-1 | `vite-plugin-pwa` 离线支持 | app shell precache + 静态资源 runtime cache；兑现离线承诺 |
| P1-2 | 种子数据按分类外置为静态 JSON | 首屏只载入默认分类，其余按需 fetch；为千条级扩容铺路 |
| P1-3 | `useFavoritesStore` 待删队列补测试 | 动云同步代码前先上保险 |
| P1-4 | 「新生工具包」场景包 + 卡片复制链接按钮 | 校园运营弹药 |
| P1-5 | `lib/data.ts` 按域拆分（纯重构，接口不变） | 后续迭代的可维护性投资 |

### P2 · 增长期（第 2 个月起，按数据反馈排期）

- 构建期预渲染资源详情页（SSG），解决百度收录与首屏 TTFB；
- 构建期简易倒排索引替换线性扫描搜索；
- 微信分享海报生成（canvas 绘制资源卡片）；
- 多校区复制指南（一校一 profile 的构建配置模板）；
- Turnstile 接入（issue #11 遗留）防机器人刷投票。

---

## 4. 低成本部署操作手册（资金紧张特供）

### 4.1 方案 A：校内网部署（¥0，主力方案）

适合：全校/全院同学主要通过校园网访问。不需要公网 IP、不需要备案、不受任何境外网络波动影响。

```bash
# 1. 本地构建（纯静态模式，不接 Supabase）
npm ci --legacy-peer-deps
VITE_BASE_URL=/ VITE_BUILD_DIR=dist \
VITE_HIDDEN_CATEGORIES=proxy \
VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= \
npm run build

# 2. 上传到校内任意一台常开机器（机房退役电脑/实验室服务器均可）
scp -r dist/ user@campus-server:/srv/openbox/
```

校内服务器侧（推荐 Caddy，单二进制零依赖）：

```text
# /etc/caddy/Caddyfile
openbox.campus.edu.cn {
    root * /srv/openbox/dist
    encode zstd gzip
    # SPA 深链回退（构建时 gen-spa-paths.mjs 已生成实体文件，此处双保险）
    try_files {path} {path}/index.html index.html
    # 带 hash 的静态资源长缓存（与 public/_headers 口径一致）
    @immutable path /assets/*
    header @immutable Cache-Control "public, max-age=31536000, immutable"
    file_server
}
```

```bash
sudo systemctl enable --now caddy   # 域名解析由校园网 DNS 添加一条内网 A 记录即可
```

无域名时直接 `http://<内网IP>:8080` 也能跑（记得告知用户非 HTTPS 环境的注意事项）。机器回收时：整站就是 `dist/` 一个目录，拷走即迁移。

### 4.2 方案 B：腾讯 EdgeOne Pages（¥0，公网门面）

1. 注册腾讯云 → 控制台搜「EdgeOne Pages」开通免费计划（个人开发者/教育项目长期免费，流量与请求均不限量）；
2. 创建项目 → 连接 Git 仓库（支持 GitHub/GitLab；也可用 CLI 直接上传 `dist`）；
3. Build configuration：
   - Install: `npm ci --legacy-peer-deps`
   - Build: `VITE_BASE_URL=/ VITE_BUILD_DIR=dist npm run build`
   - Output: `dist`
4. 环境变量（可选）：`NODE_VERSION=20`；公网版若接 Supabase 再加两个 `VITE_SUPABASE_*` 变量；
5. 推送 main 即自动构建上线，默认获得 `https://<项目名>.edgeone.app`，大陆访问质量远优于 pages.dev。

### 4.3 方案 C：Cloudflare Pages（保留为海外镜像）

现状即可用：控制台 Connect to Git（无需 API Token），build 同上。不再新增投入；如坚持走 Actions 自动线，恢复 `deploy.yml` 的 push 触发并确认 Secrets 未过期。

### 4.4 Supabase 决策（省钱核心）

| 场景 | 决策 |
|---|---|
| 校内版 | **不用**。`VITE_SUPABASE_*` 留空 → 纯静态 + localStorage 兜底，零运维、零数据合规负担、零费用 |
| 公网版 | 可选用免费档 + 已有 0001–0008 迁移；国内延迟问题用 `VITE_SUPABASE_PROXY_URL`（CF Worker 反代）缓解 |
| 未来多校复制 | 每校独立 Supabase 项目（免费档）或干脆各校纯静态，投稿走 Issue 模板人工合入 |

---

## 5. 技术债登记簿（按优先级）

| 优先级 | 事项 | 来源 |
|---|---|---|
| 高 | PWA 离线承诺未兑现 | slogans vs 无 SW |
| 高 | 自动部署链断裂（deploy.yml 禁用 + pages.dev 不可达） | commit `25c40b7` |
| 高 | 校园语境分类可见性不可配置 | 合规评估 |
| 中 | 种子数据进首屏 bundle | sites-data chunk |
| 中 | `lib/data.ts` 六域耦合 671 行 | 架构组 A3 |
| 中 | 收藏云同步并发逻辑零测试 | 架构组 A4 |
| 中 | monitor.yml 注释宣称的触发链已失效 | 文档一致性 |
| 低 | 搜索线性扫描 | 数据 ×3 前不致命 |
| 低 | `VITE_ADMIN_EMAILS` 打进产物 | RLS 有兜底 |
| 低 | wrangler.toml docs/dist 双口径注释 | 易混淆不影响功能 |
| 低 | SUPABASE_SETUP.md 96–101 行 markdown 围栏残缺 | 文档 |

---

## 6. 风险登记册

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 校内机器被回收/重装 | 中 | 校内服务中断 | 纯静态目录随时可迁；EdgeOne 版兜底 |
| EdgeOne 免费政策收紧 | 低 | 公网点失联 | Cloudflare 版仍在线；纯静态随处可搬 |
| 校园网审计关注敏感分类 | 中 | 个人/社团被约谈 | P0-3 分类过滤，校内版只留干净分类 |
| 免费资源大面积死亡 | 中 | 信任受损 | monitor 流水线 + 黑名单机制已在位 |
| 个人时间不足以维持每周更新 | 高 | 内容陈旧流失 | 降低更新标准（每周 3–5 条精品 > 凑数）；吸收社团成员共同维护 |

---

## 7. 实施记录（2026-08-26 · P0 全量 + P1 核心项）

| 计划项 | 状态 | 落点 |
|---|---|---|
| P0-1 EdgeOne 接入 | ✅ 文档就绪（控制台操作需账号，无法代办） | `EDGEONE_DEPLOY.md` |
| P0-2 校内部署包 | ✅ | `DEPLOY-SCHOOL.md`（含 Caddy/Nginx/systemd/最简四档） |
| P0-3 隐藏分类全链路 | ✅ 实测：304→226 路由目录、sitemap 零泄漏、公网版不受影响 | `taxonomy.ts` / `seed.ts` / `data.ts` / `gen-spa-paths.mjs` |
| P0-4 文档一致性 | ✅ | README 路由描述、monitor.yml 注释、HANDOFF 增补、SUPABASE_SETUP 围栏、`.env.example` |
| P0-5 纯静态构建验证 | ✅ 双模式产物核查通过（sw.js / manifest / 目录清单） | 本节证据命令 |
| P1-1 PWA 离线兑现 | ✅ vite-plugin-pwa 1.3；vitest 兼容修复（test 模式跳过插件） | `vite.config.ts`；`public/manifest.json` 迁入插件配置 |
| P1-2 数据外置 JSON | ⏸ 未做（280 条规模尚可，千条级前完成即可） | — |
| P1-3 favorites 待删队列测试 | ✅ 5 用例（失败入队/成功清队/重试成功/重试仍败/未登录零调用） | `useFavoritesStore.test.ts` |
| P1-4a 新生工具包场景包 | ✅ freshman 场景 + learn/tools/ai-apps/freechat 映射 + Backpack 图标 | `taxonomy.ts` / `Icon.tsx` |
| P1-4b 复制链接覆盖 | ✅ Card/DetailView 原已有，补齐 Row 形态 | `ResourceRow.tsx` |
| P1-5 data.ts 拆分 | ⏸ 未做（纯重构，另行安排） | — |

**验证证据**：`tsc -b --noEmit` 通过 · `eslint .` 退出码 0 · `vitest run` 51/51 通过（新增 9 例）·
双模式构建产物核查（隐藏分类泄漏检查 0 命中）· agentseed 双工具非 blocking。

**遗留提醒**：① EdgeOne/GitHub 远程与控制台操作需站长手动完成；② bundle 内营销文案的惰性字符串残留见 `DEPLOY-SCHOOL.md` 第 7 节。

---

## 8. 实施记录 · 第二轮（2026-08-26 深夜 · 三线并查）

### 8.1 链接全量巡检（check-links，662 个 URL 字面量）

| 结论 | 数值 | 判读 |
|---|---|---|
| alive | 254 | — |
| blocked（反爬 403/429/999） | 37 | 站点大概率活着 |
| dead | 40 | **多为 API 端点裸 GET 的天然 404**（siliconflow/dashscope/moonshot/nvidia 等 `/v1` base），非真死链 |
| server-error | 7 | 530/522 = CF 源站宕，与既有 audit 记录吻合 |
| unreachable | 324 | **本机出口受限噪声**（仅 github.com 就占 38 条），不作数 |

**按纪律处置：单次探测不改任何资源状态**（monitor 的连续 2 次判死机制继续把关）。
真正修掉的是巡检工具自身的缺陷：tips 散文里的裸链接字面量粘连中文备注
（`…/v1——存活，不判死`）被整段当 URL 探测 → `extractUrls` 在首个 CJK 字符处截断，
实测提取 662→661、污染清零。人工标记待复核项：`api.tokenriver.cn`（404，与在营的
tokenriver.ai 疑似重复条目）、`caipacity` 条目的 `api.capacity.com` 域名存疑——留给下一轮 monitor 双确认。

### 8.2 安全专项

`npm audit fix` 修复 nanoid <3.3.18 高危（自定义生成器 size=0 死循环），修复后 **0 vulnerabilities**。

### 8.3 P1-5 上帝模块拆分 ✅

`lib/data.ts`（725 行）→ 门面 + 六域：
```
src/lib/data.ts          # 门面：export * 全量再导出，调用方零改动
src/lib/data/shared.ts       # withSupabase 统一降级通道 + SubmissionRow 归一化
src/lib/data/resources.ts    # 查询/筛选/LRU 缓存/社区投稿合并/隐藏分类闸门
src/lib/data/submissions.ts  # 投稿双重校验+查重预检、失效反馈
src/lib/data/verifications.ts# 验证投票：设备指纹、单条/批量统计、23505 去重
src/lib/data/comments.ts     # 评论：云端+本地兜底并集、我的评论
src/lib/data/admin.ts        # 待审队列、通过/拒绝、管理员预判
```
回归：tsc / eslint / vitest 51 用例全绿；13 个消费组件零改动。

### 8.4 P1-2 数据外置：实测后缓做（工程裁决）

首屏关键 chunk 实测（gzip 后）：**sites-data 仅 21.55 KB**、index 12.85 KB、icons 8.66 KB、react-vendor 57.3 KB。
即全部种子数据上线传输成本 ≈ 一张小图。拆分按分类 JSON 的收益上限是这 21KB 的一部分，
代价是 fetch 瀑布、SW 预缓存清单膨胀、离线兜底复杂化三重成本；
且首页聚合视图（榜单/精选/标签云/场景树）本就需要全量数据，分类切片帮不了首屏。
**裁决：缓做。重估阈值：gzip 后数据 >100KB（约 1200+ 条）或引入服务端搜索时再启动。**

### 8.5 移动端/a11y

浏览器级实测（playwright 视口截图/scrollWidth 检测）本机环境不具备条件，本轮未做，不假装做了。
代码级现状（此前审计已确认）：Modal 有完整焦点陷阱/Esc/焦点归还，触控目标 ≥36px，
safe-area 已适配——结构性无硬伤；专项实测列入下轮待办。

### 8.6 GitCode Pages 可行性结论（回应「国内品牌部署」之问）

**可行，定位为实验性第四轨。** 证据：官方文档 `gitcode.com/cocoachina/pages/overview`
（"GitCode Pages 提供静态网址服务"）、2026 年多篇第三方实战记录（含踩坑帖）、
访问形态 `<user>.gitcode.io/<repo>`、免费 + 自定义域名。
保留意见：构建管线成熟度与稳定性证据弱于 EdgeOne（多数实战为推静态产物分支模式），
故矩阵中标注实验性；**国内公网主力仍是 EdgeOne Pages，校内场景仍是内网部署**。
开通路径（待站长账号实测）：推送 `dist/` 至专用分支 → 仓库设置启用 Pages → 绑定/使用默认域。

### 8.7 对接准备状态

- **数据库**：`SUPABASE_SETUP.md` 顶部新增 ⚡快速对接清单（7 步表）；迁移链 0001–0008 幂等可重跑；
  校内版建议保持纯静态（零合规负担），公网版按清单接入。
- **仓库**：本地已补配 `github` 远程（github.com/Morningstar202604/OpenBox），现有四远程
  origin(gitee)/gitcode/github/(push 时按需带令牌)；未做任何推送。

---

*本评估基于 2026-08-25 代码库状态（commit `25c40b7`）。部署渠道政策以各家官网当期为准。*
